# T13: Enhanced Diagnostics & Field Selector

*Created: 2026-07-31 14:55 IST*
*Last Updated: 2026-07-31 18:13 IST*
**Attribution**: Implemented by GPT-5.6 Terra (Codex); original review inputs from GPT-5.6 Terra (OpenAI) and Sage (OpenClaw)

**Status**: 🔄 **IN PROGRESS** — implementation delivered; long-run and mobile acceptance remain
**Priority**: MEDIUM

## Overview

Add diagnostic metrics and a field selector that enhance the simulator's pedagogical value without cluttering the primary interface. These are refinements that build on the three-view foundation.

## Subtasks

### T13a: Compact Numerical Diagnostics

**New/renamed metrics**:

| Current | New | Formula | Notes |
|---------|-----|---------|-------|
| — | Courant number | `λ = c·dt/dx` (classical) or `Δτ/Δσ` (relativistic) | Stability indicator |
| "Causality" | Max transverse speed | `max_σ |∂y/∂τ|` | Rename for clarity |
| — | Energy drift | `(E(τ) − E(0)) / E(0)` | Conservation check in relativistic mode |

**Acceptance**: All metrics update in real time; energy drift stays < 1% for long runs in relativistic mode.

---

### T13b: Energy Strip Labeling

- Make energy density strip **optional** (toggle in settings)
- Label explicitly: `ℰ(σ, τ₀)` with formula:
  ```
  ℰ = ½μ(∂τy)² + ½τ(∂σy)²
  ```
- Use **sequential palette** for energy (nonnegative) vs diverging for displacement

---

### T13c: Worldsheet Field Selector

A dropdown or button group above the worldsheet to select the displayed field:

| Field | Symbol | Meaning | Best Use |
|-------|--------|---------|----------|
| Transverse displacement | `y` | Default | Wave propagation |
| Transverse velocity | `∂τ y` | Time derivative | Turning points, moving packets |
| Energy density | `ℰ` | Local energy | Energy flow, localization |
| Local slope | `∂σ y` | Space derivative | Steepening, mode structure |

**Implementation**: Each field is derived from the stored history buffer. For `∂τ y`, compute finite differences in time; for `∂σ y`, compute finite differences in space.

**Color mapping**:
- Signed fields (`y`, `∂τ y`, `∂σ y`): diverging palette, dark neutral at 0, cyan positive, magenta negative
- Nonnegative field (`ℰ`): sequential palette
- Fixed symmetric scale `[-y_max, y_max]` over displayed temporal window

---

### T13d: Worldsheet Color Bar

- Compact vertical bar on right side of worldsheet
- Numerical endpoints labeled
- Shows the mapping from color to field value

## Dependencies

- T10, T11, T12

## Delivered Slice

- Total energy, wave speed, fundamental frequency, and relativistic causality refresh every advancing animation frame.
- Energy is displayed to five decimals; a damping check demonstrated dynamic loss.
- The remaining requested diagnostics and field-selector features are still pending.

## 2026-08-14 Implementation

- Replaced the misleading relativistic “Causality” label with Courant number and maximum transverse-speed diagnostics; added relativistic-only energy drift since reset with green/amber/red thresholds at 1% and 5%.
- Made the profile energy-density strip optional and persisted its visibility. Its scale is now sequential because energy is nonnegative.
- Added a persisted worldsheet selector for displacement, transverse velocity, energy density, and local slope. Signed fields use a symmetric cyan/magenta scale; energy uses a sequential dark-to-warm scale. The renderer draws a labelled colour bar from the rolling visible window.
- Added matching glossary/help entries for the selected field, maximum transverse speed, and energy drift.
- `npm run build`, `npm test`, and `git diff --check` passed. Desktop browser verification confirmed selector updates, persisted field state, colour-bar rendering, energy-drift visibility in relativistic mode, and no console errors.

## Remaining Acceptance

- Run a longer relativistic regression and confirm energy drift remains below 1% for representative presets.
- Manually check the selector, colour bar, diagnostics, and energy-strip toggle on a mobile viewport before marking T13 complete.

## Related

- `implementation-details/field-selector.md`
- `implementation-details/energy-strip-labeling.md`
- `implementation-details/diagnostics-courant-maxspeed.md`
- Terra's review (Point 3, 4, 7)
