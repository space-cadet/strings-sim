# T12: Probe Trajectory & Interaction

*Created: 2026-07-31 14:55 IST*
*Last Updated: 2026-08-04 18:36:51 IST*
**Attribution**: GPT-5.6 Terra (OpenAI), Deepak Vaid mockups

**Status**: 🔄 **IN PROGRESS** — implementation complete; manual acceptance test pending
**Priority**: HIGH

## Overview

Add a **draggable probe point** on the string profile that tracks a fixed `σ*` position. Its history is plotted as `y(τ; σ*)` — a proper graph with `τ` horizontal and `y` vertical. This replaces the current "Relativistic History" panel which is actually a compressed and misleading trace.

## Features

### 1. Draggable Probe on Profile
- A cyan dot on the `y(σ, τ₀)` profile at the selected `σ*`
- Click or drag on the profile to move `σ*`
- Color identifies the same point across all three views

### 2. Probe Trajectory Panel
- Horizontal axis: `τ` (time), rolling window matched to worldsheet
- Vertical axis: `y(σ*, τ)`
- One vivid trace for the selected probe point
- Optional faint traces for two additional pinned probes
- Small horizontal line at `y = 0`
- Optional: `|dy/dτ| = 1` diagonal guide, clearly labeled

### 3. Worldsheet Linkage
- Thin vertical line at `σ = σ*` in the worldsheet heatmap
- The selected worldline is highlighted
- Clicking on the worldsheet sets `σ*`

### 4. Trajectory History Buffer
- Store `y[probeIndex]` at each time step
- Rolling buffer matched to worldsheet temporal window
- Decouple from full worldsheet history (the probe needs only one scalar per timestep)

## Files to Modify

- `src/main.ts` — Add probe state, event handling, trajectory buffer
- `src/visualization/renderer.ts` — Draw probe marker on profile
- New: `src/visualization/probe-trajectory.ts` — Trajectory plot renderer
- `src/visualization/worldsheet.ts` — Draw vertical probe line

## API Sketch

```typescript
interface ProbeState {
  sigmaIndex: number;     // grid index of probe
  sigma: number;          // physical position σ*
  history: Float64Array;  // y values over time
  color: string;          // e.g. '#00d4ff' (cyan)
}
```

## Dependencies

- T10 (bug fixes)
- T11 (three-view layout) — trajectory panel must be placed

## Delivered Implementation

- Added a selected probe at fixed $\sigma_\ast$, chosen by pointer interaction on either the profile or the worldsheet; keyboard left/right arrows adjust the selected grid point when a canvas is focused.
- Linked the cyan profile marker, dashed worldsheet worldline, trajectory status, and rolling graph of $y(\sigma_\ast,\tau)$.
- Added a bounded 200-sample, timestamped scalar buffer that records after each completed solver step and resets when the selected coordinate or simulation state resets.
- Added `src/visualization/probe-trajectory.ts` and focused unit coverage for selection reset and bounded-history behaviour.

## Verification

- `npm test` passed, including the probe-state tests.
- `npm run build` passed.
- Browser checks passed for desktop and $390 \times 844$ mobile layouts, with populated cyan traces and no console errors.
- Evidence: `memory-bank/screenshots/2026-08-04-181614-probe-trajectory-desktop.png`, `memory-bank/screenshots/2026-08-04-181614-probe-trajectory-mobile.png`, and `memory-bank/ui-tests/2026-08-04-181614-probe-trajectory.md`.

## Remaining Acceptance

- User manual testing is still required before T12 is marked complete.
- Secondary pinned probes and a clearly labelled $|\partial_\tau y|=1$ guide remain out of scope for this delivered primary-probe slice.

## Related

- `implementation-details/probe-trajectory.md`
- Terra's review (Point 2, 3, 6)
